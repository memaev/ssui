# OS
.DS_Store
Thumbs.db

# IDE (project-level; each sub-project also has its own .gitignore)
.idea/
.vscode/
*.iml

# Local machine config
**/local.properties

# Scratch files at the repo root (screen JSON experiments); real screens live in demo/ or the backend seed
/*.json
